{
  "name": "hyderabad-metro-go",
  "version": "2.0.0-preview.1",
  "private": true,
  "type": "module",
  "description": "A clean-slate, map-led Hyderabad Metro journey planner.",
  "scripts": {
    "serve": "python3 -m http.server 3000",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/app.js && node --check src/data/network.js && node --check src/domain/route-engine.js && node --check src/motion.js"
  },
  "engines": {
    "node": ">=20"
  }
}
